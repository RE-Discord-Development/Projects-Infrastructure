- name: Deploy the application stack to a server
  hosts: localhost

  vars_files:
   - "vars/secrets.yml"
   - "vars/values.yml"

  roles:
    - docker
    - traefik
    - prometheus
    - grafana
    - communitybot